{
  "name": "ai-media-analysis-tool",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && cross-env NODE_OPTIONS=--openssl-legacy-provider npm install && cross-env NODE_OPTIONS=--openssl-legacy-provider npm run build",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsdom": "^25.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
